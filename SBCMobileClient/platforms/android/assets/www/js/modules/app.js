/**
 * New node file
 */
var app = angular.module('sbcApp', [])
.directive('sbcApp',['$window', '$document', 
    function($window, $document) {
		return function(scope, element, attr)
		{
	
			
		};
}]);
